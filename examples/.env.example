# Copy to `examples/.env` and fill in your key.
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-...

# Optional: the examples also work against OpenAI directly.
# Swap `openrouter(...)` for `openai(...)` in the example source.
# OPENAI_API_KEY=sk-...
