{
  "name": "example-built-in-tools",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The built-in tool pack: automatic pure tools, keyless web tools, and a sandboxed filesystem.",
  "scripts": {
    "start": "node --env-file-if-exists=../.env --import tsx index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "just-another-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.20.6",
    "typescript": "^6.0.3"
  }
}
