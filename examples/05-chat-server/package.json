{
  "name": "example-chat-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A streaming chat backend: toResponse(), sessions per user, tool events, and undo.",
  "scripts": {
    "start": "node --env-file-if-exists=../.env --import tsx index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "just-another-sdk": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.20.6",
    "typescript": "^6.0.3"
  }
}
